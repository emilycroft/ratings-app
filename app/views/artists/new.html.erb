<h1>Create a New Artist</h1>

<% @artist.errors.full_messages.each do |msg| %>
<p><%= msg %></p>
<% end %>

<%= form_for(@artist) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.submit %>
<% end %>
